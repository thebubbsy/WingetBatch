## 2024-05-14 - Prevent duplicate command output in CLI details
**Learning:** Designated copy-pasteable commands should only appear once (typically at the end) to avoid visual clutter and confusion.
**Action:** Ensure that CLI detail views display the primary action command (like `winget install`) only once, positioned logically at the end of the output.
